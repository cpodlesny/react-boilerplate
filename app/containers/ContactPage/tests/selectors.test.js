import {
  selectContactPage,
} from '../selectors';
import { fromJS } from 'immutable';
import expect from 'expect';

const selector = selectContactPage();

describe('selectContactPage', () => {

});
